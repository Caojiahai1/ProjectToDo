<!--index.wxml-->
<view>
  <!-- 顶部搜索框 -->
  <view class="topFixedView"  >
    <wux-search-bar clear show-cancel placeholder="Search" bind:change="onChange" bind:focus="onFocus" bind:blur="onBlur" bind:confirm="onConfirm" bind:clear="onClear" bind:cancel="onCancel" />
  </view>
  <!-- <scroll-view class="topFixedView" style="height:54px"></scroll-view> -->
  <!-- <view style="margin-top:{{marginTop}}">
    <wux-divider position="left" text='任务列表'/>
  </view> -->
  <!-- 任务列表显示 -->
  <view style="margin-top:{{marginTop}}">
    <wux-row wx:for="{{ taskList }}" wux-class="taskStyle" gutter="0">
      <wux-col wux-class="taskList1" span="2"><wux-icon type="ios-checkbox-outline" size="20" /></wux-col>
      <wux-col wux-class="taskList2" span="10"><text>{{item.text}}\n{{item.date}}</text></wux-col>
    </wux-row>
  </view>

  <wux-fab-button class="fab-button" theme="positive" direction = "vertical" buttons="{{buttons}}" />

  <!-- <cover-view class="addTask" style="background-color:{{taskBgColor}}"
   bindtouchstart="addTask" bindtouchend="moveTask">+</cover-view> -->

  <wux-cell-group class="addTaskInput" style='bottom:{{inputBottom}}px;display:{{inputDisplay}};'>
    <wux-cell hover-class="none">
        <wux-textarea
          center
          clearable
          placeholder="请输入"
          adjust-position= "{{false}}"
          bindfocus="inputFocus"
          bindblur="inputblur"
          focus="{{inputShowed}}" />
    </wux-cell>
  </wux-cell-group>
  <!-- <button bindtap="addTask">111</button> -->

  <!-- <van-cell-group class="addTaskInput"
  style='bottom:{{inputBottom}}px;display:{{inputDisplay}};' >
  <van-field
    custom-style="background-color: #6491CB;padding:8px 5px"
    value="{{ sms }}"
    center
    clearable
    placeholder="请输入"
    adjust-position= "{{false}}"
    bindfocus="inputFocus"
    bindblur="inputblur"
    focus="{{inputShowed}}"
    use-button-slot
  >
    <van-button slot="button" size="small" type="primary" custom-style="background-color:#234678;border:0px">保存</van-button>
  </van-field>
</van-cell-group> -->
  
</view>
